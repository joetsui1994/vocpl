def display_version(String version) {
    println(
        """
        ================================================
                Global-VOCs Pipeline version ${version}
        ================================================
        """.stripIndent()
    )
}
